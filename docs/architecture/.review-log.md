# Architecture Review Log

## 2026-04-12 — Architecture review of BLI-189 (nearby data architecture v2)

**Issues found:** 2 (0 critical, 0 high, 1 medium, 1 low)
**Key findings:**
- MEDIUM: Group member counts hardcoded to 0 in `getNearbyMapMarkers` — placeholder, not a bug but should be documented
- LOW: `processProximityStatusMatching` sends empty `matchedUserIds` array (by design, but asymmetric with `processStatusMatching`)
**Aligned:** Soft-delete filtering, block filtering, status visibility, rate limit separation, columnar response, bbox viewport filter, supercluster integration all correctly follow architecture. Docs updated in same PR.
**Patterns:** First split-endpoint review — no recurring pattern yet.

## 2026-04-12 Compile (re-run, post-BLI-210/211/212)

**Scope:** Full `/architecture-compile` re-run in isolated worktree (`.worktrees/arch-compile-2026-04-12b`) on `origin/main` @ `ad13c09`, after the 3 follow-up fixes from BLI-209's compile merged (BLI-210 soft-delete, BLI-211 dead dedup, BLI-212 pair_key). 9 parallel domain-scan agents + schema-drift agent + invariant-drift agent + discovery/PRODUCT/CLAUDE agent.

**Outcome:** 30 docs verified, 0 doc drift. 1 code fix applied (group push collapseId). Schema drift clean (migration 0021 resolved `waves_pending_unique`). 9/10 invariants clean.

### Fix applied
- **push.ts + messages.ts** — group message first-unread push now always sets `collapseId: "group:{conversationId}"` with explicit `sound: true` override. Previously, first-unread omitted collapseId (audible but non-replaceable), allowing concurrent first-messages to stack audible notifications. Fix decouples sound from collapseId via a new optional `sound: boolean` field on `sendPushToUser` payload. All existing callers backward-compatible (default: audible when no collapseId, silent when collapseId set). push-notifications.md updated to reflect the new behavior.

### Invariants verified clean
- No sync AI calls in procedures (0 hits)
- Soft-delete filter on discovery queries verified (BLI-210 fix confirmed in place)
- No star selects (0 hits)
- No `notInArray` usage (0 hits)
- `ee.emit` only in documented typing exception (`ws/handler.ts`)
- Private `currentStatus`/`statusCategories` never sent to LLM (filter before prompt)
- Block check ordering correct in BLI-212 waves.send (block check fires before onConflictDoNothing)
- `pair_key` invariants clean (STORED GENERATED, never written by app, onConflictDoNothing atomic)
- `waves.send` rate limit still wired post-BLI-212

### Schema drift (step 5b)
- CLEAN. Migration 0021 (`waves_active_unique`) deployed. `waves_pending_unique` dropped in prod. `pair_key` column in sync. All 44 indexes, 26 FKs match.

### Quality scores
- No material change from BLI-209 scores. All implemented-feature docs ≥98.

### Proposed Rules (carried from BLI-209, all resolved)
- `arch-docs/index-sync` — **ALREADY EXISTS** as `architect/index-synced` (added in BLI-209 compile)
- `migrations/append-history` — **REJECTED** — migration files are self-documenting; duplicating their history in database.md adds maintenance burden without value
- `push/group-always-collapse` — **RESOLVED** via code fix (BLI-215): collapseId now always set for group pushes; sound decoupled via `sound: boolean` override. No rule needed — the code is self-enforcing

---

## 2026-04-12 Compile (BLI-209)

**Scope:** Full `/architecture-compile` run executed in an isolated worktree (`.worktrees/bli-209-arch-compile-v2`) on a fresh clone of `origin/main` (commit `0cfef65`). 9 parallel domain-scan agents + 1 prod-vs-Drizzle schema-drift agent (step 5b). The run was redone in the worktree after a parallel session contaminated the main working tree mid-compile — earlier results were discarded and re-derived from clean files.

**Outcome:** ~14 docs accurate, ~14 outdated (now fixed in this PR), 0 missing. 1 prod schema drift item flagged but deferred (separate PR).

### Drift fixed in this PR
- **rate-limiting.md + waves-connections.md** — 4 rate-limit numbers had drifted 10x (waves.send 30→300, waves.respond 60→600, profiles.getNearby 30→600, global 200→2000). Both docs updated. Per Karol, the inflated values are a temporary mitigation for [BLI-189](https://linear.app/blisko/issue/BLI-189) (map burns through bucket because `getNearbyUsersForMap` lacks client-side debounce/dedup); the doc explicitly notes the long-term values to revert to once BLI-189 lands.
- **account-deletion.md + data-export.md + ai-matching.md + status-matching.md** — stale `ai-jobs` queue name (post-BLI-171 cross-reference miss). Renamed to `ops` / `ai` as appropriate.
- **account-deletion.md** — `forceDisconnect` was described as a single-process EventEmitter broadcast; corrected to Redis `publishEvent` (cross-replica) — important for multi-replica WS correctness. Also added `metrics.aiCalls` to the Step 3 anonymization table (was only listing `metrics.requestEvents`).
- **database.md** — appended migrations 0019 + 0020 to history table; corrected stale `apps/api/src/db/schema.ts` path references to `packages/db/src/schema.ts` (the schema was moved to the `@repo/db` workspace package; `apps/api/src/db/schema.ts` is now a re-export wrapper).
- **infrastructure.md** — added `apps/tasks/` (dev-only Bun.serve task viewer), missing root scripts (`api:dev:production`, `api:scatter:production`, `api:seed:slo`, `ralph`/`ralph:dry`, `dev-cli` monitor commands).
- **messaging.md** — documented `muteConversation`/`unmuteConversation` (BLI-157 era, undocumented since); added per-participant `mutedUntil` push suppression interaction in push-notifications.md.
- **admin-panel.md** — added `/dashboard/ai-costs` route (BLI-174), noted `user-analyses.ts` router, fixed BullMQ description from single `enqueueAndWait` to 3-queue split (`enqueueAiAndWait`/`enqueueOpsAndWait`/`enqueueMaintenanceAndWait`).
- **mobile-architecture.md** — added Provider section (`ToastProvider`, `NotificationProvider`), Hook Catalog (9 hooks including 4 self-healing retry hooks + `useProfileGate` + `usePrefetchMessages`), SecureStore key catalog (5 keys, with corrected `blisko_notification_prefs` key name), self-healing WS failure events list.
- **push-notifications.md** — fixed group-invite row (was "audible, no collapseId"; actual is silent + `collapseId: "group-invite:{conversationId}"`); added Conversation Mute section.
- **blocking-moderation.md** — moderation error string updated from Polish text to `JSON.stringify({ error: "CONTENT_MODERATED" })`.
- **privacy-terms.md** — split last-updated date per route (privacy.tsx 8 kwietnia, terms.tsx 7 marca); flagged data-category re-audit for new fields (superpower, dateOfBirth, statusCategories, currentStatus, doNotDisturb, metrics.aiCalls).
- **`.claude/rules/architect.md`** — added 3 missing docs to architecture index (`admin-panel.md`, `ai-cost-tracking.md`, `demo-chatbot.md`); bumped websockets event count 17→22.
- **websockets-realtime.md** — clarified "22 distinct server→client types" heading vs. table-row count (which counts inbound/outbound separately).
- **onboarding-flow.md** — clarified displayName length: server validator 2-50, onboarding `TextInput` capped at `maxLength={30}` (not a contradiction, two separate constraints).
- **ai-cost-tracking.md** — function/call-site tally corrected (9 functions / 12 logged call sites); the "ctx?: AiLogCtx is optional" claim was wrong — `ctx: AiLogCtx` is required by every function signature, doc rewritten to reflect that and explain the design rationale.
- **ai-matching.md** — `removeOnComplete: true` was wrong (actual: `{count:200, age:3600}`); Debounce table said "jobId dedup" for `quick-score`/`status-matching` but they actually use BullMQ `deduplication` option; added explicit self-healing failure-event paragraph (`analysisFailed`/`statusMatchingFailed`) so this doc isn't silent on the BLI-158/164 retry pattern.
- **nearby-group-members.md** — Impact Map rate limit was `30/min` (stale); corrected to `600/min` with BLI-189 temp note.
- **data-export.md** — `statusExpiresAt`/`statusSetAt` added to "Not exported" exclusion list.

### Database schema drift (step 5b)
- **HIGH (deferred to parallel session's PR):** `waves_pending_unique` partial unique index exists in production but is not defined in `packages/db/src/schema.ts` and not in any migration under `apps/api/drizzle/`. Karol's parallel session is already adding the schema definition + migration `0021_add_waves_pending_unique_index.sql` + Drizzle schema-gotcha doc note in a separate branch. Not touched in this PR to avoid stepping on that work.
- All other tables/columns/FKs/indexes match between prod and Drizzle after dialect normalization. No other drift.

### Code bugs found (out of scope for this PR — separate tickets)
- **CRITICAL** `profiles.ts:670 getMyStatusMatches` missing `INNER JOIN user + isNull(deletedAt)` filter — soft-deleted users still surface in status matches. Violates `security/filter-soft-deleted`.
- **HIGH** `accounts.ts:116 requestDataExport` data-export 24h dedup queries the legacy `new Queue("ai-jobs", ...)` queue, which was obliterated post-BLI-171. The dedup check always passes — only the rate-limiter (`rateLimits.dataExport` 1/24h) backstops it. Pure ops bug, not a data leak.

### Invariants verified clean
- No sync AI calls (`generateObject`/`generateText`/`embed`) in `apps/api/src/trpc/procedures/`.
- No `notInArray` anywhere in `apps/api/src/`.
- No `.select()` star-selects in procedures (only relational API).
- `publishEvent` used for all cross-replica WS events; the only `ee.emit` outside the bridge is the local-only typing path in `ws/handler.ts:130` (intentional, ephemeral).
- Private `currentStatus`/`statusCategories` never sent to LLM — `processStatusMatching` filters `statusVisibility === "private"` before building the LLM prompt.
- Self-healing failure handlers cover all 5 user-facing AI job types: `analyze-pair`, `generate-profiling-question`, `generate-profile-from-qa`, `generate-profile-ai`, `status-matching`. Mobile retry hooks present for each.
- 18 BullMQ job types across 3 queues (ai/ops/maintenance), all match doc.
- 22 WS event types, all routed via `publishEvent` except documented exception.

### Patterns observed (3 candidate rules — pending user decision)
1. **`arch-docs/index-sync`** — 3 docs missing from `architect.md` index (`admin-panel.md`, `ai-cost-tracking.md`, `demo-chatbot.md`) despite existing on disk. Pattern: new architecture docs get created without updating the index table.
2. **`migrations/append-history`** — 2 migrations missed in single week (0019, 0020). Pattern: doc updates from `/architecture-update` describe the schema change in prose but skip the migration history table.
3. **`push/group-always-collapse`** — 1 inconsistency: `messages.ts:605` group push only sets `collapseId` when `hasUnread`. First-message branch sends without collapse, so concurrent first-messages can stack audible notifications. Other group pushes (group-invite, ambient match) always set collapseId.

### Quality score trends
- Lowest scoring (planned/aspirational, not actionable): `friends-system.md` 55, `monetization.md` 58. Both correctly marked as planned features.
- Biggest recoveries: `mobile-architecture.md` 98→100, `privacy-terms.md` 96→98.
- All implemented-feature docs are now ≥98 overall.

### Actions taken
- 14 architecture docs updated (worktree-isolated, fresh from origin/main).
- `.claude/rules/architect.md` index updated.
- `.quality-scores.json` overwritten with 2026-04-12 scores + trend deltas.
- 2 follow-up Linear tickets to be created for the code bugs above (BLI-209 will note them; new tickets blocked by parent compile ticket).
- 3 candidate rules presented to user; awaiting decision before encoding.
- Schema-drift item (waves_pending_unique) deferred to parallel session's PR.

### Proposed Rules
- `arch-docs/index-sync` — PENDING — 3 docs missing from index this compile, no prior occurrences logged
- `migrations/append-history` — PENDING — 2 missed this compile, no prior occurrences
- `push/group-always-collapse` — PENDING — 1 inconsistency this compile, no prior occurrences

---

## 2026-04-11 — Post-implementation review of BLI-188 (T3 promotion on tap)

**Scope:** 2-file backend + mobile fix after production diagnosis showed `analyze-pair` never firing for a real user despite repeated profile taps.

**Issues found:** 0 critical, 0 high, 2 low (both pre-existing, flagged for visibility).
**Key findings:**
- [ALIGNED] `getDetailedAnalysis` ported bidirectional block + bilateral `isComplete` gate from `getConnectionAnalysis` — switching the modal to it does not open a data-leak surface to blocked/incomplete pairs.
- [ALIGNED] Readiness checks in `ensureAnalysis` and `getDetailedAnalysis` now key off `tier === 't3'` instead of `shortSnippet IS NOT NULL`. The invariant `t3 ⟺ shortSnippet IS NOT NULL` holds today (T2 `setWhere`-guarded upsert, T3 writes both fields atomically), but tier is the explicit source of truth per BLI-184.
- [LOW, pre-existing] `ensureAnalysis` does not check blocks — a blocked pair whose modal returns `null` still triggers self-heal after 10s and enqueues `analyze-pair` work. Not a leak (returns status strings only), just wasted AI budget. Same mechanism existed pre-BLI-188 via the old `getConnectionAnalysis` path.
- [LOW, pre-existing] Soft-delete race: target user soft-deletes between map refresh and modal open → analysis procedures still load. Cross-cutting with `security.md` `security/filter-soft-deleted`; separate ticket territory.

**Docs updated in-branch:**
- `ai-matching.md` — new "Tier Invariant & Readiness Checks" section explicating the invariant and why readiness checks must use `tier`. Update marker added.
- `user-profiles.md` — `getDetailedAnalysis` added to block/isComplete consequences row and impact map row. Update marker added.

**Patterns:**
- Second regression in a week caused by conflating "row exists" with "tier ready" after BLI-185 redefined `analysisReady` semantics. Check callsites of `connection_analyses.findFirst` across the codebase for any other place that treats bare existence as T3-ready.

## 2026-04-11 — Post-implementation review of BLI-174 (AI cost tracking)

**Scope:** Full branch review after implementation, before PR creation.

**Issues found:** 2 (0 critical, 1 high, 1 medium) — both fixed in-branch before PR.
**Key findings (all addressed):**
- [HIGH] `feed` endpoint in `ai-costs.ts` router lacked a time-window filter → unbounded scan on a growing table. Fixed: added `window: WINDOW.default("7d")` parameter matching the pattern of all other breakdown endpoints.
- [MEDIUM] `evaluateStatusMatch` JSON.parse failures bypassed logging (caught by outer try/catch without a `failed` row being appended). Fixed: moved the JSON parse inside the `withAiLogging` callback with an explicit throw on malformed output, so parse failures now produce `failed` rows in the dashboard.

**Other observations (no action needed):**
- GDPR anonymization uses two separate UPDATEs for `ai_calls` — same precedent as `request_events`. Accepted as consistent.
- Vercel AI SDK v6 `usage` extraction is correct for all 9 functions (`inputTokens`/`outputTokens` for text/object, `tokens` for embeddings).
- Wrapper preserves graceful degradation across all AI functions — errors rethrow so existing fallback catch blocks still run.
- Hot-path overhead of `withAiLogging` is negligible (~2 `Date.now()` + 1 object alloc + fire-and-forget `RPUSH`) vs 100ms–2s per AI call.

**Patterns:**
- Continues the `createBatchBuffer` reuse pattern (3rd consumer: push-log, consistency-sweep, ai-calls). Pattern is battle-tested now.
- `drizzle/no-star-select` caught by `/simplify` in `feed` endpoint — consider adding Biome lint rule or pre-commit check for this.
- Admin router `resolveDisplayNames` triplication across 3 routers (push-log, queue, ai-costs) — worth extracting to a shared util in a follow-up; out of scope for this PR per scope-discipline rule.

## 2026-04-11 — Pre-implementation review of BLI-174 proposal (AI cost tracking)

**Scope:** Reviewed ticket description before plan is written. No code changes yet.

**Issues found:** 9 (0 critical, 2 high, 5 medium, 2 low)
**Key findings:**
- [HIGH] Ticket assumes raw `openai.chat.completions.create` — actual code uses Vercel AI SDK (`embed`/`generateText`/`generateObject` from `ai`). `usage` field shape differs; plan must adapt.
- [HIGH] Existing AI functions have graceful degradation (catch → fallback). Logging must live inside the try/catch and never propagate errors upward.
- [MEDIUM] `withAiLogging` wrapper inside `ai.ts` is the only clean fit for 7+ call sites — wrapping externally in workers bloats `queue.ts`.
- [MEDIUM] Not all AI calls are inside queue jobs (embedding generation can be inline). Decide how to tag `jobName` for inline calls — recommend logging all calls with `jobName: "inline"`.
- [MEDIUM] Maintenance worker registration (`upsertJobScheduler` + `MaintenanceJob` union + switchcase) missing from ticket — required by queues-jobs.md Impact Map.
- [MEDIUM] `ai-matching.md` "Cost Estimates" section will be outdated post-implementation; needs `/architecture-update`.
- [LOW] `numeric(12,6)` acceptable for per-call storage; aggregate SUM queries must cast explicitly.
- [LOW] Data export: recommend excluding `ai_calls` (parallel to `request_events`).
- [LOW] Architecture doc: recommend new `ai-cost-tracking.md` (not appended to `instrumentation.md`).

**Patterns:**
- Ticket prescriptiveness hazard: BLI-174 contains ready-to-use code snippets referencing the wrong SDK. Drafting code in tickets without checking actual imports is worth calling out — 2nd time seeing a ticket describe an SDK that diverged from actual code (1st: earlier OpenAI direct-client assumption in BLI-147).
- Push-log infrastructure reuse: 3rd ticket leveraging `createBatchBuffer` + maintenance-queue flush/prune pattern (BLI-167 push-log, BLI-168 consistency-sweep, now BLI-174). Pattern is well-proven.

## 2026-04-10 Compile (second)

- 27 docs accurate (94-100 overall), 2 placeholder docs below threshold (55-58)
- Key drift: 2 code violations fixed (group invite push missing collapseId, getConnectionAnalysis/getById missing block check), 1 soft-delete filter gap fixed (getGroupInfo memberCount)
- New patterns: file path references go stale after file moves (3rd occurrence — gdpr-compliance.md last time, account-deletion.md this time)
- Lowest scoring: `friends-system.md` at 55, `monetization.md` at 58 (both planned features, acceptable)
- Actions taken: see fixes below

### Fixes Applied

1. **account-deletion.md** — 3 stale `queue.ts` → `queue-ops.ts` file path references (lines 16, 84, 94)
2. **database.md** — added migration 0017 (`add_push_sends`) to Migration History table
3. **ai-profiling.md** — fixed moderation error message (Polish phrase → `CONTENT_MODERATED` JSON), added missing `profileFailed` WS event to events table
4. **rate-limiting.md** — added 4 self-healing retry rate limits (`profiling.retryQuestion`, `profiling.retryProfileGeneration`, `profiles.retryProfileAI`, `profiles.retryStatusMatching` — all 10/1h)
5. **groups.ts** — added `collapseId: group-invite:{conversationId}` to group invitation push notifications (lines 142, 441)
6. **profiles.ts** — added bidirectional block check to `getConnectionAnalysis` and `getById` (blocked users return null instead of data)
7. **groups.ts** — added soft-delete filter (INNER JOIN user WHERE deletedAt IS NULL) to `getGroupInfo` memberCount query

### Findings by Severity

#### CRITICAL
None

#### HIGH
- `profiles.ts:509` — `getConnectionAnalysis` returned analysis data to blocked users (fixed: bidirectional block check)
- `profiles.ts:540` — `getById` returned profile data to blocked users (fixed: bidirectional block check)

#### MEDIUM
- `groups.ts:142,441` — group invitation push missing collapseId (fixed: added `group-invite:{conversationId}`)
- `account-deletion.md` lines 16,84,94 — stale `queue.ts` references after BLI-171 queue split (fixed → `queue-ops.ts`)
- `groups.ts:703` — `getGroupInfo` memberCount didn't filter soft-deleted users (fixed: INNER JOIN user)

#### LOW
- `database.md` — migration 0017 missing from history table (fixed)
- `ai-profiling.md` — moderation error message outdated (fixed)
- `ai-profiling.md` — `profileFailed` WS event missing from events table (fixed)
- `rate-limiting.md` — 4 self-healing retry rate limits undocumented (fixed)

### PRODUCT.md Alignment
- Friends system: planned, correctly marked not implemented
- Monetization: planned, infrastructure partially ready
- Verified badge: planned, only emailVerified exists
- Android: PRODUCT.md promises iOS+Android, app is iOS-only

### Proposed Rules
- None proposed — file path staleness pattern (3 occurrences) is better addressed by grep-verification in /architecture-update rather than a static rule

## 2026-04-06 Compile (initial)

- 25 docs accurate (96-100 overall), 3 placeholder docs below threshold (45-58)
- Key drift: Polish text encoding in 3 GDPR docs (`"Usuniety"` → `"Usunięty"`), e2e-test-coverage.md critically stale (15 tests exist but all marked untested)
- New patterns: forceDisconnect uses ee.emit instead of publishEvent (cross-replica gap), dual typing triggers undocumented, star selects in profiles.ts and topics.ts
- Lowest scoring: `e2e-test-coverage.md` at 45, `friends-system.md` at 55, `monetization.md` at 58
- Actions taken: see fixes below

### Fixes Applied

1. **Polish encoding** — fixed `"Usuniety"` → `"Usunięty"` in 5 docs (account-deletion, gdpr-compliance, data-export, auth-sessions, queues-jobs)
2. **database.md** — added `profile_by_user_id` prepared statement
3. **e2e-test-coverage.md** — full rewrite with 15 actual Maestro tests, template structure, Impact Map
4. **Star selects** — `profiles.ts:335-336` converted to `db.query` with explicit columns, `topics.ts:136` converted to explicit `db.select({...})`
5. **forceDisconnect cross-replica** — `accounts.ts` changed from `ee.emit` to `publishEvent` (broadcasts via Redis to all replicas)
6. **GDPR data export** — added `statusCategories` to export, documented intentional exclusion of embeddings
7. **Message content moderation** — added `moderateContent()` call in `messages.send` for text messages, structured `CONTENT_MODERATED` error code, global toast handler on mobile
8. **messaging.md** + **blocking-moderation.md** — updated to reflect message moderation (removed gap notation)

### Findings by Severity

#### CRITICAL
- `e2e-test-coverage.md` marks all 45 flows as "untested" — 15 Maestro tests actually exist covering ~13 flows
- Polish text encoding in `account-deletion.md`, `gdpr-compliance.md`, `data-export.md`: docs say `"Usuniety uzytkownik"` but code uses `"Usunięty użytkownik"`

#### HIGH
- `data-export.ts` missing GDPR export fields: `statusCategories`, `embedding`, `statusEmbedding` (code AND doc gap)
- `websockets-realtime.md`: forceDisconnect (accounts.ts:106) uses `ee.emit` not `publishEvent` — breaks cross-replica delivery
- Star selects without explicit columns: `profiles.ts:335-336` (connectionAnalyses, statusMatches), `topics.ts:136` (topics)
- `database.md` missing 4th prepared statement: `profile_by_user_id`

#### MEDIUM
- `messaging.md` + `websockets-realtime.md`: dual typing triggers (WS message + tRPC mutation) not documented
- `analysis:ready` channel described as part of unified ws-events bridge but is actually separate Redis channel
- `blocking-moderation.md` Impact Map missing `profiling.ts` (6 moderation call sites)
- `user-profiles.md` missing location fields (latitude, longitude, lastLocationUpdate)
- `privacy-terms.md` legal document dates stale ("7 marca 2026")
- `infrastructure.md` missing `apps/tasks/` utility directory
- `mobile-architecture.md` missing hooks (useProfileGate, usePrefetchMessages), providers, SecureStore key names
- `demo-chatbot.md` missing runtime specifics (Bun, Drizzle ORM, AI SDK from Vercel)
- Various undocumented procedures: ensureAnalysis, getDetailedAnalysis, getSessionState, getSessions

### PRODUCT.md Alignment
- Friends system: planned in PRODUCT.md, correctly marked planned in docs, no code exists
- Monetization: planned in PRODUCT.md, correctly marked planned in docs, no code (but pingLimits.ts and featureGates exist as infrastructure)
- Verified badge: PRODUCT.md describes liveness check — only `emailVerified` in schema, no face verification
- Android: PRODUCT.md says "iOS i Android" — current implementation is iOS-only

### Proposed Rules
- None proposed (first compile, no recurring patterns yet)

## 2026-04-10 — Review of kwypchlo/bli-158-self-healing-ai-queue (BLI-158)

**Issues found:** 0 (0 critical, 0 high, 0 medium)
**Key findings:**
- All changes aligned: `analysisFailed` WS event documented, quick-score dedup change documented, worker failure handling section added
- Architecture docs updated before review (no gaps)
- Code review caught one issue (visibility guard on index.tsx `analysisFailed` handler) — fixed before architecture review
**Patterns:** None new

## 2026-04-10 — Review of kwypchlo/bli-162-self-healing-generate-profile-from-qa (BLI-162)

**Issues found:** 0 (0 critical, 0 high, 0 medium)
**Key findings:**
- All changes aligned: `profilingFailed` WS event documented (20 total), `generate-profile-from-qa` dedup switch documented, worker failure handler updated
- Architecture docs updated before review (3 docs: websockets-realtime.md, queues-jobs.md, ai-profiling.md)
- Shared `loadRetryContext` helper extracted from `retryQuestion` + `retryProfileGeneration` — reduces drift risk
- Code review found no real bugs (infinite retry loop is by-design, session status race is harmless due to polling fallback)
**Patterns:** Self-healing pattern now covers 3 job types (analyze-pair, generate-profiling-question, generate-profile-from-qa) — consistent approach across all

## 2026-04-10 — Update for kwypchlo/bli-163-self-healing-generate-profile-ai (BLI-163)

**Docs updated:** `queues-jobs.md`, `websockets-realtime.md`
**Changes:**
- Added `profileFailed` WS event (21 total events now)
- Added `generate-profile-ai` to worker failure handling section
- Added `profileFailed` to event routing summary
**Patterns:** Self-healing pattern now covers all 4 AI job types that users wait on (analyze-pair, generate-profiling-question, generate-profile-from-qa, generate-profile-ai)

## 2026-04-10 — Update for kwypchlo/bli-164-self-healing-status-matching (BLI-164)

**Docs updated:** `queues-jobs.md`, `websockets-realtime.md`
**Changes:**
- Added `statusMatchingFailed` WS event (22 total events now)
- `status-matching` switched from `jobId` to BullMQ `deduplication` (auto-release on failure)
- Added `status-matching` to worker failure handling section
- Added `statusMatchingFailed` to event routing summary
**Patterns:** Self-healing now covers 5 job types: analyze-pair, generate-profiling-question, generate-profile-from-qa, generate-profile-ai, status-matching

## 2026-04-10 — Update for kwypchlo/bli-165-self-healing-export-user-data (BLI-165)

**Docs updated:** `queues-jobs.md`, `data-export.md`
**Changes:**
- `export-user-data` upgraded to 10 attempts with 60s exponential backoff (~8.5h total)
- `removeOnFail: false` — failed exports persist in Redis until resolved
- On permanent failure: user delay email + admin alert email + console error
- Two new email templates: `dataExportDelayed()`, `dataExportFailedAdmin()`
**Patterns:** GDPR-critical jobs get aggressive retry + admin alerting, unlike AI jobs which use WS-based self-healing. Different failure model: system must deliver, user shouldn't need to retry manually.

## 2026-04-10 — Review of kwypchlo/bli-171-split-queue-into-three

**Issues found:** 3 (0 critical, 0 high, 3 medium)
**Key findings:**
- `gdpr-compliance.md` referenced `queue.ts` instead of `queue-ops.ts` for `processHardDeleteUser` (2 occurrences) — fixed
- `infrastructure.md` described single `ai-jobs` queue — updated to 3-queue table
- `infrastructure.md` referenced `queue.ts hard-delete` instead of `queue-ops.ts` — fixed
**Patterns:** File path references in architecture docs go stale when code moves between files. The 3-queue refactor was correctly documented in `queues-jobs.md` but cross-references in other docs were missed. Consider grep-based verification of `queue.ts` references across all docs after queue file changes.

## 2026-04-10 — Update for kwypchlo/bli-168-nightly-consistency-sweep (BLI-168)

**Docs updated:** `queues-jobs.md`
**Changes:**
- Added `consistency-sweep` as 16th job type in maintenance queue (3 types now)
- Documented nightly scheduler (cron `0 3 * * *`), admin trigger, and `runConsistencySweep()` shared function
- Updated queue architecture table (maintenance: 3 types)
**Patterns:** First "offline self-healing" — catches stuck state that WS-based retry can't reach (user offline when job failed). Complements the 5 WS-based self-healing implementations (BLI-158/161/162/163/164).

## 2026-04-11 — Review of BLI-205 / PR #136

**Branch:** `kwypchlo/bli-205-fix-push-token-lifecycle-sync`
**Issues found:** 1 (0 critical, 0 high, 0 medium, 1 low)
**Key findings:**
- LOW: `usePushNotifications` foreground sync runs on every `AppState === "active"` transition without a time-based debounce; `useBackgroundSync` uses a 10s `MIN_BACKGROUND_MS` threshold. Accepted as-is — permission sync has different semantics (needs immediate reconciliation, hot path is cheap).
**Patterns:** None recurring. This is the first architecture review on the mobile push token lifecycle area.
