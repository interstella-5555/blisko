# Architecture Review Log

## 2026-04-10 Compile (second)

- 27 docs accurate (94-100 overall), 2 placeholder docs below threshold (55-58)
- Key drift: 2 code violations fixed (group invite push missing collapseId, getConnectionAnalysis/getById missing block check), 1 soft-delete filter gap fixed (getGroupInfo memberCount)
- New patterns: file path references go stale after file moves (3rd occurrence — gdpr-compliance.md last time, account-deletion.md this time)
- Lowest scoring: `friends-system.md` at 55, `monetization.md` at 58 (both planned features, acceptable)
- Actions taken: see fixes below

### Fixes Applied

1. **account-deletion.md** — 3 stale `queue.ts` → `queue-ops.ts` file path references (lines 16, 84, 94)
2. **database.md** — added migration 0017 (`add_push_sends`) to Migration History table
3. **ai-profiling.md** — fixed moderation error message (Polish phrase → `CONTENT_MODERATED` JSON), added missing `profileFailed` WS event to events table
4. **rate-limiting.md** — added 4 self-healing retry rate limits (`profiling.retryQuestion`, `profiling.retryProfileGeneration`, `profiles.retryProfileAI`, `profiles.retryStatusMatching` — all 10/1h)
5. **groups.ts** — added `collapseId: group-invite:{conversationId}` to group invitation push notifications (lines 142, 441)
6. **profiles.ts** — added bidirectional block check to `getConnectionAnalysis` and `getById` (blocked users return null instead of data)
7. **groups.ts** — added soft-delete filter (INNER JOIN user WHERE deletedAt IS NULL) to `getGroupInfo` memberCount query

### Findings by Severity

#### CRITICAL
None

#### HIGH
- `profiles.ts:509` — `getConnectionAnalysis` returned analysis data to blocked users (fixed: bidirectional block check)
- `profiles.ts:540` — `getById` returned profile data to blocked users (fixed: bidirectional block check)

#### MEDIUM
- `groups.ts:142,441` — group invitation push missing collapseId (fixed: added `group-invite:{conversationId}`)
- `account-deletion.md` lines 16,84,94 — stale `queue.ts` references after BLI-171 queue split (fixed → `queue-ops.ts`)
- `groups.ts:703` — `getGroupInfo` memberCount didn't filter soft-deleted users (fixed: INNER JOIN user)

#### LOW
- `database.md` — migration 0017 missing from history table (fixed)
- `ai-profiling.md` — moderation error message outdated (fixed)
- `ai-profiling.md` — `profileFailed` WS event missing from events table (fixed)
- `rate-limiting.md` — 4 self-healing retry rate limits undocumented (fixed)

### PRODUCT.md Alignment
- Friends system: planned, correctly marked not implemented
- Monetization: planned, infrastructure partially ready
- Verified badge: planned, only emailVerified exists
- Android: PRODUCT.md promises iOS+Android, app is iOS-only

### Proposed Rules
- None proposed — file path staleness pattern (3 occurrences) is better addressed by grep-verification in /architecture-update rather than a static rule

## 2026-04-06 Compile (initial)

- 25 docs accurate (96-100 overall), 3 placeholder docs below threshold (45-58)
- Key drift: Polish text encoding in 3 GDPR docs (`"Usuniety"` → `"Usunięty"`), e2e-test-coverage.md critically stale (15 tests exist but all marked untested)
- New patterns: forceDisconnect uses ee.emit instead of publishEvent (cross-replica gap), dual typing triggers undocumented, star selects in profiles.ts and topics.ts
- Lowest scoring: `e2e-test-coverage.md` at 45, `friends-system.md` at 55, `monetization.md` at 58
- Actions taken: see fixes below

### Fixes Applied

1. **Polish encoding** — fixed `"Usuniety"` → `"Usunięty"` in 5 docs (account-deletion, gdpr-compliance, data-export, auth-sessions, queues-jobs)
2. **database.md** — added `profile_by_user_id` prepared statement
3. **e2e-test-coverage.md** — full rewrite with 15 actual Maestro tests, template structure, Impact Map
4. **Star selects** — `profiles.ts:335-336` converted to `db.query` with explicit columns, `topics.ts:136` converted to explicit `db.select({...})`
5. **forceDisconnect cross-replica** — `accounts.ts` changed from `ee.emit` to `publishEvent` (broadcasts via Redis to all replicas)
6. **GDPR data export** — added `statusCategories` to export, documented intentional exclusion of embeddings
7. **Message content moderation** — added `moderateContent()` call in `messages.send` for text messages, structured `CONTENT_MODERATED` error code, global toast handler on mobile
8. **messaging.md** + **blocking-moderation.md** — updated to reflect message moderation (removed gap notation)

### Findings by Severity

#### CRITICAL
- `e2e-test-coverage.md` marks all 45 flows as "untested" — 15 Maestro tests actually exist covering ~13 flows
- Polish text encoding in `account-deletion.md`, `gdpr-compliance.md`, `data-export.md`: docs say `"Usuniety uzytkownik"` but code uses `"Usunięty użytkownik"`

#### HIGH
- `data-export.ts` missing GDPR export fields: `statusCategories`, `embedding`, `statusEmbedding` (code AND doc gap)
- `websockets-realtime.md`: forceDisconnect (accounts.ts:106) uses `ee.emit` not `publishEvent` — breaks cross-replica delivery
- Star selects without explicit columns: `profiles.ts:335-336` (connectionAnalyses, statusMatches), `topics.ts:136` (topics)
- `database.md` missing 4th prepared statement: `profile_by_user_id`

#### MEDIUM
- `messaging.md` + `websockets-realtime.md`: dual typing triggers (WS message + tRPC mutation) not documented
- `analysis:ready` channel described as part of unified ws-events bridge but is actually separate Redis channel
- `blocking-moderation.md` Impact Map missing `profiling.ts` (6 moderation call sites)
- `user-profiles.md` missing location fields (latitude, longitude, lastLocationUpdate)
- `privacy-terms.md` legal document dates stale ("7 marca 2026")
- `infrastructure.md` missing `apps/tasks/` utility directory
- `mobile-architecture.md` missing hooks (useProfileGate, usePrefetchMessages), providers, SecureStore key names
- `demo-chatbot.md` missing runtime specifics (Bun, Drizzle ORM, AI SDK from Vercel)
- Various undocumented procedures: ensureAnalysis, getDetailedAnalysis, getSessionState, getSessions

### PRODUCT.md Alignment
- Friends system: planned in PRODUCT.md, correctly marked planned in docs, no code exists
- Monetization: planned in PRODUCT.md, correctly marked planned in docs, no code (but pingLimits.ts and featureGates exist as infrastructure)
- Verified badge: PRODUCT.md describes liveness check — only `emailVerified` in schema, no face verification
- Android: PRODUCT.md says "iOS i Android" — current implementation is iOS-only

### Proposed Rules
- None proposed (first compile, no recurring patterns yet)

## 2026-04-10 — Review of kwypchlo/bli-158-self-healing-ai-queue (BLI-158)

**Issues found:** 0 (0 critical, 0 high, 0 medium)
**Key findings:**
- All changes aligned: `analysisFailed` WS event documented, quick-score dedup change documented, worker failure handling section added
- Architecture docs updated before review (no gaps)
- Code review caught one issue (visibility guard on index.tsx `analysisFailed` handler) — fixed before architecture review
**Patterns:** None new

## 2026-04-10 — Review of kwypchlo/bli-162-self-healing-generate-profile-from-qa (BLI-162)

**Issues found:** 0 (0 critical, 0 high, 0 medium)
**Key findings:**
- All changes aligned: `profilingFailed` WS event documented (20 total), `generate-profile-from-qa` dedup switch documented, worker failure handler updated
- Architecture docs updated before review (3 docs: websockets-realtime.md, queues-jobs.md, ai-profiling.md)
- Shared `loadRetryContext` helper extracted from `retryQuestion` + `retryProfileGeneration` — reduces drift risk
- Code review found no real bugs (infinite retry loop is by-design, session status race is harmless due to polling fallback)
**Patterns:** Self-healing pattern now covers 3 job types (analyze-pair, generate-profiling-question, generate-profile-from-qa) — consistent approach across all

## 2026-04-10 — Update for kwypchlo/bli-163-self-healing-generate-profile-ai (BLI-163)

**Docs updated:** `queues-jobs.md`, `websockets-realtime.md`
**Changes:**
- Added `profileFailed` WS event (21 total events now)
- Added `generate-profile-ai` to worker failure handling section
- Added `profileFailed` to event routing summary
**Patterns:** Self-healing pattern now covers all 4 AI job types that users wait on (analyze-pair, generate-profiling-question, generate-profile-from-qa, generate-profile-ai)

## 2026-04-10 — Update for kwypchlo/bli-164-self-healing-status-matching (BLI-164)

**Docs updated:** `queues-jobs.md`, `websockets-realtime.md`
**Changes:**
- Added `statusMatchingFailed` WS event (22 total events now)
- `status-matching` switched from `jobId` to BullMQ `deduplication` (auto-release on failure)
- Added `status-matching` to worker failure handling section
- Added `statusMatchingFailed` to event routing summary
**Patterns:** Self-healing now covers 5 job types: analyze-pair, generate-profiling-question, generate-profile-from-qa, generate-profile-ai, status-matching

## 2026-04-10 — Update for kwypchlo/bli-165-self-healing-export-user-data (BLI-165)

**Docs updated:** `queues-jobs.md`, `data-export.md`
**Changes:**
- `export-user-data` upgraded to 10 attempts with 60s exponential backoff (~8.5h total)
- `removeOnFail: false` — failed exports persist in Redis until resolved
- On permanent failure: user delay email + admin alert email + console error
- Two new email templates: `dataExportDelayed()`, `dataExportFailedAdmin()`
**Patterns:** GDPR-critical jobs get aggressive retry + admin alerting, unlike AI jobs which use WS-based self-healing. Different failure model: system must deliver, user shouldn't need to retry manually.

## 2026-04-10 — Review of kwypchlo/bli-171-split-queue-into-three

**Issues found:** 3 (0 critical, 0 high, 3 medium)
**Key findings:**
- `gdpr-compliance.md` referenced `queue.ts` instead of `queue-ops.ts` for `processHardDeleteUser` (2 occurrences) — fixed
- `infrastructure.md` described single `ai-jobs` queue — updated to 3-queue table
- `infrastructure.md` referenced `queue.ts hard-delete` instead of `queue-ops.ts` — fixed
**Patterns:** File path references in architecture docs go stale when code moves between files. The 3-queue refactor was correctly documented in `queues-jobs.md` but cross-references in other docs were missed. Consider grep-based verification of `queue.ts` references across all docs after queue file changes.

## 2026-04-10 — Update for kwypchlo/bli-168-nightly-consistency-sweep (BLI-168)

**Docs updated:** `queues-jobs.md`
**Changes:**
- Added `consistency-sweep` as 16th job type in maintenance queue (3 types now)
- Documented nightly scheduler (cron `0 3 * * *`), admin trigger, and `runConsistencySweep()` shared function
- Updated queue architecture table (maintenance: 3 types)
**Patterns:** First "offline self-healing" — catches stuck state that WS-based retry can't reach (user offline when job failed). Complements the 5 WS-based self-healing implementations (BLI-158/161/162/163/164).
