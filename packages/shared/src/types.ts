// Shared types are intentionally minimal — only types that are actively
// imported by multiple apps belong here. The API uses Drizzle-inferred
// types from the schema; the mobile app defines its own store types.
//
// Zod validators and their inferred types live in validators.ts.
