# Blisko — Architecture

Architecture docs are in `docs/architecture/`. See `.claude/rules/architect.md` for the full index mapping docs to domains.

Skills: `/architecture-review` (code review gate), `/architecture-update` (sync docs after changes), `/architecture-compile` (deep maintenance scan).
