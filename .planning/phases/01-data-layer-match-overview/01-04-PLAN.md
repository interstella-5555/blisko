---
phase: 01-data-layer-match-overview
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - apps/admin/components.json
  - apps/admin/src/lib/utils.ts
  - apps/admin/src/components/ui/button.tsx
  - apps/admin/src/components/ui/input.tsx
  - apps/admin/src/components/ui/label.tsx
  - apps/admin/src/components/ui/sidebar.tsx
  - apps/admin/src/components/ui/separator.tsx
  - apps/admin/src/components/ui/skeleton.tsx
  - apps/admin/src/components/ui/tooltip.tsx
  - apps/admin/src/styles/app.css
autonomous: true
requirements:
  - NAVI-01

must_haves:
  truths:
    - "shadcn/ui is initialized in the admin app with CSS variables and cn() utility"
    - "All required UI components are installed: button, input, label, sidebar, separator, skeleton, tooltip"
    - "app.css contains Tailwind import and shadcn CSS variables"
  artifacts:
    - path: "apps/admin/components.json"
      provides: "shadcn/ui configuration"
      contains: "shadcn"
    - path: "apps/admin/src/lib/utils.ts"
      provides: "cn() utility function"
      contains: "twMerge"
    - path: "apps/admin/src/components/ui/sidebar.tsx"
      provides: "shadcn Sidebar component for navigation layout"
      contains: "Sidebar"
    - path: "apps/admin/src/components/ui/button.tsx"
      provides: "shadcn Button component"
      contains: "Button"
  key_links:
    - from: "apps/admin/src/components/ui/sidebar.tsx"
      to: "apps/admin/src/lib/utils.ts"
      via: "import { cn } from ~/lib/utils"
      pattern: "from.*~/lib/utils"
---

<objective>
Initialize shadcn/ui in the admin app and install all required UI component primitives.

Purpose: Per D-14, the admin app uses shadcn/ui for Table, Sheet, Badge, Sidebar, and other primitives. This plan sets up the shadcn foundation (components.json, CSS variables, cn() utility) and installs the component set needed by the sidebar layout (Plan 05) and match dashboard (Plan 06). Most files here are auto-generated by `bunx shadcn add`.
Output: shadcn/ui initialized with button, input, label, sidebar, separator, skeleton, tooltip components ready to use.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-layer-match-overview/01-CONTEXT.md
@.planning/phases/01-data-layer-match-overview/01-RESEARCH.md
@.planning/phases/01-data-layer-match-overview/01-UI-SPEC.md
@.planning/phases/01-data-layer-match-overview/01-03-SUMMARY.md

@apps/admin/src/styles/app.css
@apps/admin/tsconfig.json
@apps/admin/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn/ui and install UI components</name>
  <files>
    apps/admin/components.json,
    apps/admin/src/lib/utils.ts,
    apps/admin/src/components/ui/button.tsx,
    apps/admin/src/components/ui/input.tsx,
    apps/admin/src/components/ui/label.tsx,
    apps/admin/src/components/ui/sidebar.tsx,
    apps/admin/src/components/ui/separator.tsx,
    apps/admin/src/components/ui/skeleton.tsx,
    apps/admin/src/components/ui/tooltip.tsx,
    apps/admin/src/styles/app.css
  </files>
  <read_first>
    apps/admin/src/styles/app.css,
    apps/admin/tsconfig.json,
    apps/admin/package.json
  </read_first>
  <action>
1. Initialize shadcn/ui in the admin app. Run:
```bash
cd /Users/karol/code/blisko/apps/admin && bunx --bun shadcn@latest init
```
When prompted:
- Style: Default (New York if offered)
- Base color: Slate
- CSS variables: Yes
- Path alias: `~/` (matching tsconfig `~/*` -> `./src/*`)

If the CLI doesn't support interactive mode in this context, create `apps/admin/components.json` manually:
```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "default",
  "rsc": false,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "src/styles/app.css",
    "baseColor": "slate",
    "cssVariables": true
  },
  "aliases": {
    "components": "~/components",
    "utils": "~/lib/utils",
    "ui": "~/components/ui",
    "lib": "~/lib",
    "hooks": "~/hooks"
  },
  "iconLibrary": "lucide"
}
```

Then create `apps/admin/src/lib/utils.ts`:
```typescript
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

Install required deps: `cd /Users/karol/code/blisko/apps/admin && bun add clsx tailwind-merge`

2. Add shadcn/ui components:
```bash
cd /Users/karol/code/blisko/apps/admin && bunx --bun shadcn@latest add button input label sidebar separator skeleton tooltip
```
This generates files in `apps/admin/src/components/ui/`. If the CLI fails, manually install the components following shadcn docs.

3. Update `apps/admin/src/styles/app.css`:
- Keep the `@import "tailwindcss";` at the top
- Add shadcn/ui CSS variables block (generated by `shadcn init`). If init didn't add them, add the `:root` and `.dark` CSS variable blocks from shadcn's default theme
- Keep existing login page styles (`.login-page`, `.login-card`, etc.) for now — they'll be cleaned up in Plan 05 when login is rebuilt with shadcn
- Add sidebar-specific custom properties if needed by the shadcn sidebar component

4. Run `bun run check:fix`.
  </action>
  <verify>
    <automated>cd /Users/karol/code/blisko && test -f apps/admin/components.json && test -f apps/admin/src/lib/utils.ts && test -f apps/admin/src/components/ui/sidebar.tsx && test -f apps/admin/src/components/ui/button.tsx && test -f apps/admin/src/components/ui/input.tsx && test -f apps/admin/src/components/ui/label.tsx && test -f apps/admin/src/components/ui/separator.tsx && test -f apps/admin/src/components/ui/skeleton.tsx && test -f apps/admin/src/components/ui/tooltip.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - apps/admin/components.json exists with shadcn configuration
    - apps/admin/src/lib/utils.ts exists with `cn` function using clsx + tailwind-merge
    - apps/admin/src/components/ui/sidebar.tsx exists (shadcn-generated)
    - apps/admin/src/components/ui/button.tsx exists
    - apps/admin/src/components/ui/input.tsx exists
    - apps/admin/src/components/ui/label.tsx exists
    - apps/admin/src/components/ui/separator.tsx exists
    - apps/admin/src/components/ui/skeleton.tsx exists
    - apps/admin/src/components/ui/tooltip.tsx exists
    - apps/admin/src/styles/app.css contains shadcn CSS variables
  </acceptance_criteria>
  <done>shadcn/ui initialized with all required component primitives. CSS variables configured. cn() utility ready.</done>
</task>

</tasks>

<verification>
1. `apps/admin/components.json` exists with shadcn configuration
2. `apps/admin/src/lib/utils.ts` contains `cn` function
3. All 7 UI component files exist in `apps/admin/src/components/ui/`
4. `apps/admin/src/styles/app.css` contains CSS variables for shadcn theme
</verification>

<success_criteria>
- shadcn/ui initialized with components.json, CSS variables, and cn() utility
- All required UI components installed: button, input, label, sidebar, separator, skeleton, tooltip
- Admin app still builds after shadcn initialization
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-layer-match-overview/01-04-SUMMARY.md`
</output>
