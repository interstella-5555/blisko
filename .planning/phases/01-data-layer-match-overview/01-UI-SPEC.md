---
phase: 1
slug: data-layer-match-overview
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-26
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for the match monitoring dashboard. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (to be initialized via `bunx --bun shadcn@latest init`) |
| Preset | default (neutral) -- initialized during execution |
| Component library | Radix (via shadcn/ui) |
| Icon library | lucide-react 0.561.0 (already in project) |
| Font | System stack: `-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif` (existing pattern from `app.css`) |

**Source:** D-14 (shadcn/ui), RESEARCH.md (lucide-react), existing `app.css`

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline badge padding |
| sm | 8px | Table cell padding, compact element spacing |
| md | 16px | Default element spacing, form group margins, card padding |
| lg | 24px | Section padding, sidebar item padding |
| xl | 32px | Layout gaps between major sections |
| 2xl | 48px | Page-level vertical padding |
| 3xl | 64px | Not used in Phase 1 |

Exceptions: none

**Source:** Default 8-point scale. Existing `app.css` uses 6/12/16/20/32/40 -- normalizing to standard scale via shadcn/Tailwind spacing.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Label | 14px | 400 (regular) | 1.4 |
| Heading | 20px | 600 (semibold) | 1.2 |
| Display | 28px | 600 (semibold) | 1.2 |

**Weights used (2 total):** 400 (body text, table cells, descriptions, labels, sidebar items, timestamps, secondary info) and 600 (headings, stat card values, logo, section titles in sheet panel).

**Label vs Body differentiation:** Both are 14px/400. Labels are distinguished from body text via `text-secondary` color (`#6b6560`) and contextual placement (above inputs, as table column headers, as section headers in the sheet panel). No weight difference needed -- color and position provide sufficient distinction.

**Monospace:** Tabular nums for all numeric values in the match table (scores, job counts, durations). Use `font-variant-numeric: tabular-nums` on table columns with numbers.

**Source:** Existing `app.css` uses 13/14/20/22px range. Collapsed 13px and 14px into a single 14px step to maintain perceptible scale separation. Label/body differentiation handled by color, not weight.

---

## Color

### Light Content Area (main body)

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#f5f4f1` | Page background |
| Surface | `#ffffff` | Cards, table container, sheet panel |
| Border | `#e5e2dc` | Card borders, table row dividers |
| Text primary | `#1a1a1a` | Headings, table text, primary copy |
| Text secondary | `#6b6560` | Timestamps, secondary info, descriptions, labels |
| Text muted | `#8b8680` | Placeholders, disabled text |

### Sidebar (dark region -- 30% of layout)

| Role | Value | Usage |
|------|-------|-------|
| Background | `#0f172a` (slate-900) | Sidebar background |
| Text active | `#f8fafc` (slate-50) | Active nav item, user email |
| Text inactive | `#94a3b8` (slate-400) | Inactive nav items, disabled placeholders |
| Hover | `#1e293b` (slate-800) | Nav item hover state |
| Active indicator | `#e2e8f0` (slate-200) | Active nav item background (10% opacity) |

### Accent (10%) -- reserved elements

| Role | Value | Reserved For |
|------|-------|-------------|
| Accent | `#1a1a1a` (near-black) | Primary CTA buttons (login "Zaloguj sie"), active nav indicator background |
| Score high | `#16a34a` (green-600) | Match scores >= 70% |
| Score medium | `#ca8a04` (yellow-600) | Match scores 40-69% |
| Score low | `#dc2626` (red-600) | Match scores < 40% |
| Destructive | `#dc2626` (red-600) | Failed job badges, error states |
| Info | `#2563eb` (blue-600) | Active/waiting job count badges |

**Score color thresholds:** `>= 70` green, `>= 40` yellow, `< 40` red. Applied to score Badge and score number in the sheet panel.

**Source:** D-15 (dark sidebar, light content), existing `app.css` colors (`#f5f4f1`, `#1a1a1a`, `#e5e2dc`, `#8b8680`), D-10 (match score color coding).

---

## Component Inventory

### shadcn/ui Components Required

| Component | Usage |
|-----------|-------|
| `table` | Match analyses data table (D-08) |
| `sheet` | Match detail slide-in panel from right (D-09) |
| `badge` | Status badges (completed/failed/active), score badges |
| `sidebar` | Fixed left navigation (D-15, D-17) |
| `button` | Login CTA, logout, pagination controls |
| `input` | Login email field, OTP field |
| `label` | Form labels |
| `card` | Queue health stat cards (D-13) |
| `separator` | Visual dividers in sheet panel |
| `skeleton` | Loading states for table and stat cards |

### Custom Components

| Component | Description |
|-----------|-------------|
| `app-sidebar.tsx` | Sidebar with nav sections, user footer, logout |
| `match-table.tsx` | Paginated table of connection analyses with row click |
| `match-detail-sheet.tsx` | Sheet panel with score, AI reasoning, telemetry |
| `queue-health-cards.tsx` | 4 stat cards: waiting, active, completed, failed |
| `score-badge.tsx` | Badge with color-coded match score |
| `status-badge.tsx` | Badge showing analysis completion status |

---

## Layout Contract

### Page Structure

```
+------------------+--------------------------------------------------+
|                  |                                                  |
|    Sidebar       |    Main Content Area                             |
|    w-[240px]     |    flex-1                                        |
|    fixed left    |    p-24px (lg)                                   |
|    h-screen      |                                                  |
|    bg-slate-900  |    +------------------------------------------+  |
|                  |    | Queue Health Cards (4x stat cards)        |  |
|    [Logo]        |    | grid grid-cols-4 gap-16px                 |  |
|    [Nav items]   |    +------------------------------------------+  |
|                  |                                                  |
|    - Matches *   |    gap-24px (lg)                                 |
|    - Ops (off)   |                                                  |
|    - Users (off) |    +------------------------------------------+  |
|    - API (off)   |    | Match Analyses Table                      |  |
|                  |    | bg-white, border, rounded-12px            |  |
|    [spacer]      |    |                                          |  |
|                  |    | Pair | Score | Status | Time              |  |
|    [user email]  |    | row (clickable) ...                      |  |
|    [Logout btn]  |    |                                          |  |
|                  |    | Pagination controls at bottom             |  |
+------------------+--------------------------------------------------+
```

### Sidebar Navigation Items

| Label | Icon (lucide) | State | Route |
|-------|---------------|-------|-------|
| Matches | `Activity` | Active (Phase 1) | `/_authed/dashboard` |
| Ops | `BarChart3` | Disabled, `text-slate-600`, `cursor-not-allowed` | -- |
| Users | `Users` | Disabled, `text-slate-600`, `cursor-not-allowed` | -- |
| API | `Terminal` | Disabled, `text-slate-600`, `cursor-not-allowed` | -- |

**Source:** D-16 (disabled placeholders for future sections). Icons are at Claude's discretion per CONTEXT.md.

### Queue Health Cards (D-13)

4 cards in a row, each showing:

| Card | Label | Icon (lucide) | Value Color |
|------|-------|---------------|-------------|
| Waiting | Oczekujace | `Clock` | `text-yellow-600` |
| Active | Aktywne | `Loader` | `text-blue-600` |
| Completed | Zakonczone | `CheckCircle` | `text-green-600` |
| Failed | Nieudane | `XCircle` | `text-red-600` |

Each card: white background, border `#e5e2dc`, rounded-8px, padding 16px. Label at 14px/400 `text-secondary`, value at 28px/600 (display size).

Auto-refresh: poll every 15 seconds via `setInterval` + fetch. Show subtle pulse animation on the card that changed.

### Match Table (D-08)

| Column | Width | Content | Format |
|--------|-------|---------|--------|
| Pair | flex-1 | `{fromName} -> {toName}` | 14px/400 |
| Score | 80px | `{aiMatchScore}%` | Score badge (color-coded) |
| Status | 100px | Badge: `Completed` / `Failed` | Status badge |
| Time | 140px | Relative time (`2 min ago`) | 14px/400, `text-secondary` |

- Page size: 25 rows (discretion per CONTEXT.md -- 25 keeps the table compact without excessive scrolling)
- Pagination: Previous/Next buttons at bottom right, page indicator `Strona 1 z N`
- Row hover: `bg-gray-50` transition
- Row click: opens Sheet panel (D-09)
- Active row (sheet open): `bg-blue-50` highlight

### Match Detail Sheet (D-09, D-10, D-11)

Slides in from the right. Width: `w-[480px]`.

**Section layout (top to bottom):**

1. **Header:** `{fromName} & {toName}` as SheetTitle
2. **Score:** Score badge (large, color-coded) with percentage
3. **Summary** (section label 14px/400 `text-secondary`):
   - `shortSnippet` text, 14px/400
4. **Full Analysis** (section label):
   - `longDescription` text, 14px/400, `whitespace-pre-wrap`
5. **Profile Hashes** (section label):
   - Two-column layout: `From: {hash}` / `To: {hash}`, monospace, 14px
6. **Telemetry** (section label):
   - For completed analyses (from DB): `createdAt` and `updatedAt` timestamps
   - For failed jobs (from BullMQ): enqueued time, wait time, processing time, total duration, attempts count, job ID, error message, stack trace in `pre` block with `bg-red-50 p-16px rounded-8px text-14px overflow-x-auto`
7. **Trigger** (section label, if `triggeredBy` available):
   - Badge showing trigger source: `wave:send`, `profile:update`, `script:scatter`

Spacing between sections: 24px (lg). Each section label has 8px (sm) margin-bottom.

---

## Interaction States

### Login Page

| State | Behavior |
|-------|----------|
| Email step | Email input + "Wyslij kod" button. Button disabled while loading. |
| OTP step | OTP input (centered, spaced digits) + "Zaloguj sie" button + "Zmien adres email" link. |
| Loading | Button text changes to "Wysylanie..." / "Weryfikacja...", button disabled. |
| Error | Red error text (`#c0392b`) below input, above button. |
| Success | Redirect to `/_authed/dashboard`. |

### Dashboard

| State | Behavior |
|-------|----------|
| Loading (initial) | Skeleton loaders for queue cards (4 rectangles) + table (5 placeholder rows). |
| Data loaded | Queue cards with values + table with rows. |
| Empty table | "Brak analiz do wyswietlenia" centered in table area with `Activity` icon (muted). |
| Row hover | `bg-gray-50` background transition (150ms). |
| Row active (sheet open) | `bg-blue-50` persists while sheet is open. |
| Sheet opening | Slide in from right, 200ms ease-out. Backdrop with `bg-black/20`. |
| Sheet closing | Slide out to right, 150ms ease-in. |
| Pagination disabled | "Poprzednia" disabled on page 1, "Nastepna" disabled on last page. `opacity-50 cursor-not-allowed`. |
| Queue refresh | Subtle value transition (no full card re-render). Every 15s. |

### Sidebar

| State | Behavior |
|-------|----------|
| Active item | White text, `bg-white/10` background, left border accent `2px solid white`. |
| Disabled item | `text-slate-600`, `cursor-not-allowed`, no hover effect. Tooltip on hover: "Dostepne wkrotce". |
| Logout button | `text-slate-400`, hover `text-white`. No confirmation dialog (single admin, low risk). |

---

## Copywriting Contract

All UI copy is in Polish (consistent with existing admin app).

| Element | Copy |
|---------|------|
| Login page title | `BLISKO` |
| Login page subtitle | `Panel administracyjny` |
| Email label | `Adres email` |
| Email placeholder | `admin@blisko.app` |
| Send OTP button | `Wyslij kod` |
| Send OTP loading | `Wysylanie...` |
| OTP sent message | `Kod wyslany na {email}` |
| OTP label | `Kod weryfikacyjny` |
| OTP placeholder | `000000` |
| Login button | `Zaloguj sie` |
| Login loading | `Weryfikacja...` |
| Change email link | `Zmien adres email` |
| Auth error (generic) | `Wystapil blad. Sprobuj ponownie.` |
| Auth error (invalid OTP) | `Nieprawidlowy kod. Sprobuj ponownie.` |
| Auth error (not admin) | `Brak dostepu. To konto nie ma uprawnien administratora.` |
| Page heading | `Analiza matchow` |
| Queue section label | (no label -- cards are self-describing) |
| Queue card: waiting | `Oczekujace` |
| Queue card: active | `Aktywne` |
| Queue card: completed | `Zakonczone` |
| Queue card: failed | `Nieudane` |
| Table column: pair | `Para` |
| Table column: score | `Wynik` |
| Table column: status | `Status` |
| Table column: time | `Czas` |
| Status badge: completed | `Ukonczone` |
| Status badge: failed | `Nieudane` |
| Pagination previous | `Poprzednia` |
| Pagination next | `Nastepna` |
| Pagination indicator | `Strona {n} z {total}` |
| Empty state heading | `Brak analiz` |
| Empty state body | `Nie znaleziono zadnych analiz matchow. Analizy pojawia sie tutaj, gdy uzytkownik wyslij fale lub zaktualizuje profil.` |
| Sheet section: summary | `Podsumowanie` |
| Sheet section: full analysis | `Pelna analiza` |
| Sheet section: profile hashes | `Hashe profili` |
| Sheet section: telemetry | `Telemetria` |
| Sheet section: trigger | `Zrodlo` |
| Telemetry: created | `Utworzono` |
| Telemetry: updated | `Zaktualizowano` |
| Telemetry: wait time | `Czas oczekiwania` |
| Telemetry: processing time | `Czas przetwarzania` |
| Telemetry: total duration | `Calkowity czas` |
| Telemetry: attempts | `Proby` |
| Telemetry: job ID | `ID zadania` |
| Telemetry: error | `Blad` |
| Telemetry: stack trace | `Stack trace` |
| Sidebar: matches | `Matches` |
| Sidebar: ops | `Ops` |
| Sidebar: users | `Uzytkownicy` |
| Sidebar: api | `API` |
| Sidebar: disabled tooltip | `Dostepne wkrotce` |
| Sidebar: logout | `Wyloguj sie` |

**Destructive actions in Phase 1:** None. No data modification, no job retry (Phase 2), no delete. Logout is the only "destructive" action and requires no confirmation (single admin, immediate re-login via OTP).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | table, sheet, badge, sidebar, button, input, label, card, separator, skeleton | not required (official registry) |

No third-party registries declared.

**Source:** D-14, RESEARCH.md installation commands. Only shadcn official components.

---

## Responsive Behavior

Desktop-only. No responsive breakpoints needed.

**Source:** REQUIREMENTS.md Out of Scope: "Mobile-responsive layout -- Desktop-only admin usage"

Minimum viewport: 1280px wide. Below that, no layout adaptation (acceptable degradation).

---

## Accessibility Notes

- All form inputs have associated `<label>` elements
- Sheet panel traps focus when open (handled by Radix Dialog primitive)
- Table rows are interactive: `role="button"`, `tabIndex={0}`, `onKeyDown` for Enter/Space
- Color-coded scores always include the numeric percentage (never color-only)
- Disabled sidebar items have `aria-disabled="true"` and tooltip text
- Score badges use `aria-label` with full text: e.g. `aria-label="Wynik: 85 procent, wysoki"`

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
